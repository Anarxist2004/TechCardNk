"""
Генерация Word-документа: глава «Тестирование» (связный текст, прошедшее время).

Запуск из корня beck: python scripts/generate_testing_chapter_past_tense_doc.py
"""

from __future__ import annotations

from datetime import datetime
from pathlib import Path

from docx import Document
from docx.enum.text import WD_LINE_SPACING
from docx.shared import Pt


ROOT = Path(__file__).resolve().parents[1]
OUTPUT = ROOT / "docs" / "testing_chapter_past_tense.docx"


def _set_body_style(doc: Document) -> None:
    style = doc.styles["Normal"]
    style.font.name = "Times New Roman"
    style.font.size = Pt(12)
    pf = style.paragraph_format
    pf.line_spacing_rule = WD_LINE_SPACING.ONE_POINT_FIVE
    pf.space_after = Pt(6)


def _p(doc: Document, text: str) -> None:
    doc.add_paragraph(text)


def _heading(doc: Document, text: str, level: int) -> None:
    doc.add_heading(text, level=level)


def build_document() -> Document:
    doc = Document()
    _set_body_style(doc)

    doc.add_heading(
        "Глава. Тестирование программного обеспечения серверной части приложения",
        0,
    )
    _p(
        doc,
        f"Текст подготовлен для включения в пояснительную записку к дипломному проекту. "
        f"Дата оформления: {datetime.now().strftime('%d.%m.%Y')}.",
    )

    _heading(doc, "3.x. Цели и общая постановка задачи", 1)
    _p(
        doc,
        "В ходе выполнения дипломной работы была поставлена задача обеспечить "
        "доказуемое качество серверной реализации системы формирования операционной "
        "технологической карты радиографического контроля сварных соединений. Для этого "
        "была выделена отдельная деятельность по автоматизированному тестированию: "
        "предполагалось не только зафиксировать ожидаемое поведение ключевых компонентов, "
        "но и снизить риск регрессий при последующих доработках кода. Учитывалась "
        "многослойная структура приложения — репозиторий, сервис, конвейер преобразований, "
        "контроллер и веб-адаптер, — поэтому проверки были спроектированы так, чтобы "
        "охватить несколько уровней абстракции без обязательного подключения к "
        "промышленной базе данных на каждом запуске.",
    )

    _heading(doc, "3.x. Выбор средств и организация среды", 1)
    _p(
        doc,
        "В качестве основного инструмента был выбран фреймворк pytest для языка Python: "
        "он уже широко применялся в экосистеме проекта и позволял сочетать лаконичные "
        "тестовые функции с гибкими фикстурами и наглядными отчётами. Была подготовлена "
        "конфигурация запуска: в корне серверного модуля был размещён файл pytest.ini с "
        "указанием путей импорта и каталога tests, чтобы тесты стабильно обнаруживались "
        "при выполнении команды из рабочей директории разработчика. Дополнительно был "
        "оформлен перечень зависимостей для разработки (requirements-dev.txt), куда была "
        "включена необходимая версия pytest; таким образом, воспроизводимость среды была "
        "доведена до явной процедуры установки.",
    )

    _heading(doc, "3.x. Структура созданного набора тестов", 1)
    _p(
        doc,
        "Все автоматические проверки были сосредоточены в отдельном пакете tests, что "
        "отделило исполняемые сценарии контроля от прикладного кода и упростило навигацию "
        "по репозиторию. В общем модуле conftest.py были определены повторно используемые "
        "элементы: в частности, был реализован вспомогательный класс NoOpChanger — "
        "преобразователь данных без побочных эффектов, который позволил корректно "
        "инициализировать конвейер PipeLine на нулевой фазе и избежать ложных отказов при "
        "проверке сервисного слоя, где обработка шаблона техкарты неизбежно вызывала "
        "метод process.",
    )
    _p(
        doc,
        "Для модели TechCardData были написаны тесты сериализации и десериализации, "
        "поиска блоков и параметров, а также операций вставки и защиты от дублирования имён "
        "параметров. Эти проверки подтвердили, что внутреннее представление карты, с которым "
        "работают десятки преобразователей, оставалось предсказуемым при типичных "
        "манипуляциях структуры.",
    )
    _p(
        doc,
        "Конвейер PipeLine был исследован отдельным набором сценариев: были воспроизведены "
        "ошибочные ситуации с некорректными индексами, проверен порядок вызова нескольких "
        "преобразователей на одной фазе и убеждались, что после прохода выполнялась "
        "сортировка вложенных параметров, как это заложено в реализации.",
    )
    _p(
        doc,
        "Сервис TechCardService был протестирован с применением подставного объекта "
        "репозитория (MagicMock с ограничением по интерфейсу IRepository). Такой приём "
        "позволил имитировать ответы базы данных — например, пустой или заполненный "
        "перечень операций для блока «ПЕРЕЧЕНЬ ОПЕРАЦИЙ РК» — и при этом не требовал "
        "живого подключения к PostgreSQL. Были проверены формирование шаблона, проход "
        "pipeline при обновлении карты, нормализация имени при сохранении и корректное "
        "делегирование вызовов методам сохранения и загрузки списка техкарт.",
    )
    _p(
        doc,
        "Класс ControllerWeb был охвачен тестами с подставной реализацией IServise: "
        "удостоверились, что метод получения шаблона возвращал сериализованное JSON-представление, "
        "что обновление карты сопровождалось десериализацией входного словаря в TechCardData "
        "и передачей результата сервису, а операции сохранения и выборки сохранённых записей "
        "просто пробрасывались без искажения контракта.",
    )
    _p(
        doc,
        "Наконец, был выбран один содержательный преобразователь предметной области — "
        "CircButtWeldBeadFromThicknessTable, работающий по табличным данным без обращения к "
        "СУБД. Для него были написаны параметризованные проверки разбора числовой толщины, "
        "выборки пары величин по диапазону номинала, заполнения полей для стыкового "
        "кольцевого и листового типов соединения, а также сценарий отсутствия изменений при "
        "невыбранном скалярном значении типа шва (когда в поле передавался справочный список). "
        "Таким образом, была продемонстрирована проверка не только инфраструктурных классов, "
        "но и фрагмента инженерной логики, близкой к расчётной части дипломной темы.",
    )

    _heading(doc, "3.x. Выполнение прогонов и интерпретация результатов", 1)
    _p(
        doc,
        "После реализации перечисленных сценариев неоднократно выполнялись полные прогоны "
        "тестового набора из корня серверного модуля. Все тесты завершались успешно: "
        "отсутствовали как синтаксические и импортные ошибки, так и расхождения между "
        "ожидаемым и фактическим поведением. Полученный результат был воспринят как "
        "подтверждение внутренней согласованности описанных уровней на момент фиксации "
        "версии кода; при этом сохранялось понимание, что дальнейшее расширение функционала "
        "потребовало бы дополнения набора проверок (например, интеграционных тестов с "
        "тестовой базой или HTTP-клиентом для маршрутов FastAPI).",
    )

    _heading(doc, "3.x. Оформление отчётности", 1)
    _p(
        doc,
        "Параллельно с разработкой тестов была подготовлена текстовая отчётность: "
        "автоматизированным скриптом формировался документ Word с описанием состава каталога "
        "tests, использованных технологий и фрагментом вывода pytest на момент генерации. "
        "Данная глава дополнила материалы пояснительной записки связным изложением в "
        "прошедшем времени и акцентом на содержательной стороне проделанной работы, а не "
        "только на перечне файлов.",
    )

    _heading(doc, "3.x. Выводы по разделу", 1)
    _p(
        doc,
        "В результате была выполнена поставленная в рамках дипломного проекта работа по "
        "тестированию серверной части приложения: был создан структурированный набор "
        "автоматических проверок, охватывающий модель данных, конвейер, сервис и контроллер "
        "с применением имитаций зависимостей, а также отдельный блок предметных тестов без "
        "подключения к базе. Была выстроена воспроизводимая среда запуска, подтверждена "
        "работоспособность сценариев на момент сдачи версии и подготовлена основа для "
        "дальнейшего наращивания покрытия по мере развития системы.",
    )

    return doc


def main() -> None:
    OUTPUT.parent.mkdir(parents=True, exist_ok=True)
    doc = build_document()
    doc.save(OUTPUT)
    print(f"Written: {OUTPUT}")


if __name__ == "__main__":
    main()
