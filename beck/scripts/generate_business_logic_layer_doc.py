"""
Генерация объёмного Word-документа: слой бизнес-логики приложения
(операционная технологическая карта РК, расчёты, таблицы, правила пересчёта).

Запуск: python scripts/generate_business_logic_layer_doc.py
"""

from __future__ import annotations

from datetime import datetime
from pathlib import Path

from docx import Document
from docx.enum.text import WD_LINE_SPACING
from docx.shared import Pt


ROOT = Path(__file__).resolve().parents[1]
OUTPUT = ROOT / "docs" / "business_logic_layer_detailed.docx"


def _set_body_style(doc: Document) -> None:
    style = doc.styles["Normal"]
    style.font.name = "Times New Roman"
    style.font.size = Pt(12)
    pf = style.paragraph_format
    pf.line_spacing_rule = WD_LINE_SPACING.ONE_POINT_FIVE
    pf.space_after = Pt(6)


def _p(doc: Document, text: str, *, bullet: bool = False) -> None:
    doc.add_paragraph(text, style="List Bullet" if bullet else None)


def _heading(doc: Document, text: str, level: int) -> None:
    doc.add_heading(text, level=level)


def _add_table(doc: Document, headers: list[str], rows: list[list[str]]) -> None:
    table = doc.add_table(rows=1, cols=len(headers))
    hdr = table.rows[0].cells
    for i, h in enumerate(headers):
        hdr[i].text = h
    for row in rows:
        cells = table.add_row().cells
        for i, cell in enumerate(row):
            cells[i].text = cell


def build_document() -> Document:
    doc = Document()
    _set_body_style(doc)

    doc.add_heading(
        "Слой бизнес-логики системы формирования операционной технологической карты "
        "радиографического контроля сварных соединений",
        0,
    )
    _p(
        doc,
        (
            "Документ сформирован автоматически по исходному коду проекта. "
            f"Дата: {datetime.now().strftime('%d.%m.%Y %H:%M')}. "
            "Назначение текста — подробно описать не «архитектурные диаграммы», "
            "а предметную логику: какие параметры карты существуют, от чего они зависят, "
            "какие таблицы и формулы используются при пересчёте, в каком порядке "
            "применяются правила и что происходит при смене входных данных."
        ),
    )

    _heading(doc, "1. Роль слоя бизнес-логики в приложении", 1)
    _p(
        doc,
        "Слой бизнес-логики в данном проекте отвечает за преобразование "
        "структуры «операционной технологической карты» (ОТК РК) из шаблонного "
        "состояния в заполненное и согласованное состояние после действий пользователя "
        "(выбор типа соединения, схемы просвечивания, материала, аппарата и т.д.). "
        "Он не реализует транспорт HTTP и не выполняет прямые SQL-запросы из контроллеров: "
        "доступ к данным инкапсулирован в репозитории, а бизнес-правила сосредоточены "
        "в сервисе и наборе специализированных преобразователей (changers), "
        "последовательно применяемых к модели карты.",
    )
    _p(
        doc,
        "Ключевая идея: карта представляется как изменяемый документ "
        "(блоки и параметры), а бизнес-логика — как детерминированный конвейер "
        "обновлений, который на каждом проходе приводит значения полей в соответствие "
        "с выбранными пользователем опциями и с расчётными соотношениями, "
        "зафиксированными в коде (нормативные таблицы, эмпирические пороги по напряжению, "
        "геометрические формулы для расстояний до источника и т.п.).",
        bullet=True,
    )

    _heading(doc, "2. Модель данных технологической карты", 1)
    _p(
        doc,
        "Центральная структура — класс TechCardData. Он хранит словарь блоков, "
        "каждый блок имеет имя и набор параметров. Параметр — это, как правило, "
        "словарь с полями name, val, иногда options, subtitle, typeData, "
        "а также служебными метаданными для отслеживания происхождения значения "
        "(например, флаги параметров, полученных из базы по типу сварного соединения).",
    )
    _p(
        doc,
        "Шаблон карты создаётся методом TechCardService.crateTemplateTechCars: "
        "формируются блоки «ОПЕРАЦИОННАЯ ТЕХНОЛОГИЧЕСКАЯ КАРТА…», «Объект контроля», "
        "«ИСХОДНЫЕ ДАННЫЕ», «ПЕРЕЧЕНЬ ОПЕРАЦИЙ РК» и заполняется начальный набор полей. "
        "Далее из базы подставляется перечень операций (если есть строки для list_id).",
    )
    _p(
        doc,
        "Операции из базы: метод _apply_operations_from_db полностью заменяет "
        "стандартные операции в блоке «ПЕРЕЧЕНЬ ОПЕРАЦИЙ РК», если в таблице есть данные: "
        "для каждой строки создаётся параметр с именем из name_param (или «Операция N»), "
        "значениями val и val2. Это отделяет «справочное наполнение» от расчётной логики, "
        "но оба механизма относятся к предметной области ОТК.",
    )

    _heading(doc, "3. Конвейер пересчёта (PipeLine)", 1)
    _p(
        doc,
        "Класс PipeLine хранит упорядоченный список преобразователей IDataChanger. "
        "Метод process вызывает changeData для каждого changer на заданном индексе стадии, "
        "после чего выполняется сортировка параметров внутри блоков (sort_all_params). "
        "В текущей конфигурации все changers добавлены на индекс 0, то есть выполняются "
        "в порядке регистрации в main.create_pipeline.",
    )
    _p(doc, "Порядок выполнения в приложении (как в main.py):", bullet=True)
    order = [
        "ControlMethodsFromDb — методики контроля из БД.",
        "RegulatoryDocumentsFromDb — нормативные документы из БД.",
        "TypeOfWeldedJointFromDb — типы сварных соединений.",
        "WeldedJointDiagramFromDb — схема (рисунок) для выбранного типа шва.",
        "ControlSchemesFromJointTypeDb — допустимые схемы просвечивания по типу шва.",
        "MaterialsFromDb — материал(ы) и связанные опции.",
        "RengenApparatusFromDb — каталог ИИИ, фильтрация по предельному кВ, запись предела в поле напряжения.",
        "ParamsByWeldedJointFromDb — динамические параметры «Объект контроля» по типу шва (синхронизация с БД).",
        "CircButtWeldBeadFromThicknessTable — геометрия валика по таблице S→(e,g) для двух типов швов.",
        "ControlSensitivityChanger — чувствительность по уровню качества и толщине.",
        "RengenApparatusForPanoramicScheme — панорамная схема: фильтр аппаратов по фокусу, подсказка f.",
        "RengenDistanceForDoubleWallScheme — схема «через две стенки» (7.2): нижняя граница f.",
        "RengenDistanceForEllipseFrontalScheme — схема 7.3 «на эллипс»: f ≥ (2ΦdS)/K.",
        "RectilinearDrDistance — прямолинейное соединение (ЦР): f по СТО Газпром (разд. 9.5.x).",
        "SensitivityEtalonByMaterial — номер проволочного эталона по материалу и чувствительности.",
        "RadiographicFilmFromDb — тип плёнки из БД.",
        "IntensifyingScreenByVoltage — толщина усиливающего экрана по кВ.",
        "ProtectiveScreenByVoltage — толщина защитного экрана по кВ.",
        "ControlledZoneWidthStub — заглушка/заготовка под ширину контролируемой зоны.",
    ]
    for line in order:
        _p(doc, line, bullet=True)

    _heading(doc, "4. Детальное описание расчётной логики и таблиц", 1)

    _heading(doc, "4.1. Чувствительность контроля (ControlSensitivityChanger)", 2)
    _p(
        doc,
        "Входы: «Уровень качества» (A/B/C) и «Радиационная толщина, мм». "
        "Уровень качества маппится на «класс контроля»: A→2, B→1, C→3. "
        "По таблице SENSITIVITY_TABLE выбирается строка с наименьшей верхней границей "
        "толщины max_thickness, для которой выполняется условие thickness_mm ≤ max_thickness; "
        "значение чувствительности берётся из словаря по классу. Результат записывается "
        "в параметры с именами «Чувствительность контроля, мм» или «Чувствительность контроля».",
    )
    sens_headers = ["Верхняя граница t, мм", "Класс 1", "Класс 2", "Класс 3"]
    sens_rows = [
        ["5.0", "0,10", "0,10", "0,20"],
        ["9.0", "0,20", "0,20", "0,30"],
        ["12.0", "0,20", "0,30", "0,40"],
        ["20.0", "0,30", "0,40", "0,50"],
        ["30.0", "0,40", "0,50", "0,60"],
        ["40.0", "0,50", "0,60", "0,75"],
        ["50.0", "0,60", "0,75", "1,00"],
        ["70.0", "0,75", "1,00", "1,25"],
        ["100.0", "1,00", "1,25", "1,50"],
        ["140.0", "1,25", "1,50", "2,00"],
        ["200.0", "1,50", "2,00", "2,50"],
        ["300.0", "2,00", "2,50", "—"],
        ["400.0", "2,50", "—", "—"],
    ]
    _add_table(doc, sens_headers, sens_rows)
    _p(
        doc,
        "Примечание: для классов 2 и 3 на последних строках таблицы в коде значения могут "
        "отсутствовать — тогда подстановка не выполняется для такой комбинации.",
    )

    _heading(doc, "4.2. Предельное напряжение на трубке и фильтрация аппаратов", 2)
    _p(
        doc,
        "Модуль xray_max_kv_by_material_table определяет максимально допустимое напряжение "
        "U_max для текущей карты: запрашивается у IVoltageTubeDB по материалу и толщине. "
        "Толщина берётся в приоритете из «Номинальная толщина стенки, S, мм» (блок «Объект контроля»), "
        "иначе из «Радиационная толщина, мм» (блок «ИСХОДНЫЕ ДАННЫЕ»). Материал — из поля "
        "материала (логика MaterialsFromDb). При нехватке данных U_max = 0.",
    )
    _p(
        doc,
        "В поле «Напряжение на рентгеновской трубке, не более, кВ» записывается именно "
        "рассчитанный/справочный предел (строка через format_table_kv_text), а не напряжение "
        "выбранного аппарата.",
    )
    _p(
        doc,
        "Фильтрация каталога rengen_apparatus: для каждой строки извлекается числовое "
        "значение voltage_on_tube. Если оно разборчиво и строго больше U_max — аппарат исключается. "
        "Если напряжение не удаётся распарсить — строка сохраняется (не блокируем выбор из-за шума в данных).",
    )

    _heading(doc, "4.3. Геометрия валика по номинальной толщине (CircButtWeldBeadFromThicknessTable)", 2)
    _p(
        doc,
        "Условие применения: блок «Объект контроля», задан тип сварного соединения и числовая "
        "«Номинальная толщина стенки, S, мм». Для типа «Стыковое кольцевое. Автоматическая, односторонняя» "
        "по таблице диапазонов S находятся e и g; e записывается в «Ширина верхнего валика, А/A, мм», "
        "g — в высоты h1 и h2. Для «Листовое сварное соединение» те же e и g идут в «e, не более» и «g». "
        "Вне таблицы значения полей не меняются.",
    )
    bead_headers = ["S_min, мм", "S_max, мм", "e, мм", "g, мм"]
    bead_rows = [
        ["2", "2", "7", "1.5"],
        ["3", "4", "8", "1.5"],
        ["5", "5", "10", "1.5"],
        ["6", "8", "13", "1.5"],
        ["9", "10", "15", "1.5"],
        ["12", "12", "18", "1.5"],
        ["14", "14", "22", "1.5"],
        ["16", "16", "24", "2.0"],
        ["18", "18", "26", "2.0"],
        ["20", "20", "29", "2.0"],
        ["25", "30", "39", "2.0"],
        ["35", "40", "50", "2.0"],
    ]
    _add_table(doc, bead_headers, bead_rows)

    _heading(doc, "4.4. Панорамное просвечивание (RengenApparatusForPanoramicScheme)", 2)
    _p(
        doc,
        "Активируется, если выбрана схема «Панорамное просвечивание кольцевого сварного соединения». "
        "Вводятся наружный диаметр D, толщина стенки S_st, чувствительность K. "
        "Внутренний диаметр d = D − 2·S_st. Дополнительно требуется d/D ≥ 0.8 (иначе расчёт не ведётся).",
    )
    _p(
        doc,
        "Ограничение на размер фокусного пятна Φ (из каталога аппарата): "
        "Δ = D − d. Основной предел: Φ ≤ (K·d)/(2·Δ). Ослабленный (с пометкой в структуре данных): "
        "Φ ≤ (K·d)/Δ. Аппараты, не удовлетворяющие даже ослабленному пределу, исключаются из options.",
    )
    _p(
        doc,
        "После выбора допустимого аппарата подсказка для расстояния f: "
        "f_min = Φ·(D − d)/K, f_max = d/2, выводится как «f_min<f<=f_max» (в строке подсказки знаки "
        "могут отображаться в нотации кода). Напряжение в карте снова выставляется из предела voltage_tube.",
    )

    _heading(doc, "4.5. Кольцевое соединение через две стенки — схема 7.2 (RengenDistanceForDoubleWallScheme)", 2)
    _p(
        doc,
        "Активируется для схемы, содержащей фрагменты «кольцевое сварное соединение» и «через две стенки», "
        "но не путается со схемой «на эллипс» (её обрабатывает отдельный changer).",
    )
    _p(
        doc,
        "Обозначения: D — наружный диаметр, S_st — толщина стенки, d = D − 2·S_st, K — чувствительность, "
        "Φ — фокусное пятно (максимум из поля), класс изображения Q ∈ {A,B,C} с множителями S: A→1.2, B→1.1, C→1.5. "
        "Радиационная толщина для выбора коэффициента c берётся как t_rad = 2·S_st.",
    )
    _p(
        doc,
        "Множитель m для c = m·Φ/K зависит от Q и t_rad (логика _resolve_c_factor в коде). "
        "Итоговая нижняя граница (в коде min_distance): "
        "f_low = 1.2 · c · S · S_st − (D + d)/2, с отсечением f_low < 0. Подсказка поля: «f>=f_low».",
    )

    _heading(doc, "4.6. Фронтальное просвечивание «на эллипс» — схема 7.3 (RengenDistanceForEllipseFrontalScheme)", 2)
    _p(
        doc,
        "Условие: в названии схемы присутствуют «фронтальное просвечивание» и «на эллипс». "
        "После проверки полноты вводимых данных: f_min = (2 · Φ · d · S) / K, где S — множитель класса A/B/C "
        "как в схеме 7.2, d — внутренний диаметр. Подсказка: «f>=f_min».",
    )

    _heading(doc, "4.7. Прямолинейное сварное соединение, цифровая радиография (RectilinearDrDistance)", 2)
    _p(
        doc,
        "Условие: схема содержит «прямолинейн» и «сварн». Используются t — радиационная толщина, "
        "Φ — фокус, K — чувствительность, класс Q→S_image (1.2 / 1.1 / 1.5). "
        "Опционально b — расстояние от ППД до поверхности (поиск параметра по фрагментам «детектор»/«поверхност»), "
        "по умолчанию b = 0.",
    )
    _p(
        doc,
        "Множитель n в зависимости от Q и t задаётся функцией _n_phi_over_k_divisor. "
        "Далее t_sum = t + b; f_sto = n·Φ·S_image·t_sum/K; f_four = 4·t_sum; "
        "f_min = max(f_sto, f_four). Дополнительно в подсказку включается L_exposure_max = 0.8·f_min "
        "с отсылкой к п. 9.5.21 (как в коде).",
    )

    _heading(doc, "4.8. Усиливающий и защитный экраны по напряжению", 2)
    _p(
        doc,
        "IntensifyingScreenByVoltage: по числовому U из поля напряжения — "
        "U ≤ 100 → «До 0,02»; U ≤ 300 → «0,05-0,09»; иначе «0,09».",
    )
    _p(
        doc,
        "ProtectiveScreenByVoltage: U ≤ 200 → «До 1,0»; иначе «От 1,0 до 2,0». "
        "При неразборчивом U целевые поля обнуляются.",
    )

    _heading(doc, "4.9. Эталон чувствительности по материалу (SensitivityEtalonByMaterial)", 2)
    _p(
        doc,
        "По материалу из верхнего блока определяется material_standard_id через БД. "
        "По текущей числовой чувствительности K подбирается номер эталона из WIRE_ETALON_TABLE: "
        "для каждого номинала проволоки задаётся допуск _wire_tolerance, эталон подходит, если "
        "K ≤ nominal + tolerance. Итоговая строка: «<id_standard><номер> провол.».",
    )

    _heading(doc, "5. Синхронизация параметров объекта контроля по типу шва", 1)
    _p(
        doc,
        "ParamsByWeldedJointFromDb решает задачу согласованности: при смене типа сварного соединения "
        "не должны оставаться «чужие» параметры от предыдущего типа. Параметры, добавленные из БД, "
        "помечаются метаданными (флаг происхождения и привязка welded_joint_binding к строковому значению типа). "
        "Алгоритм: удалить помеченные параметры, отсутствующие в актуальном списке имён для текущего типа; "
        "для требуемых имён — обновить подписи/метаданные с сохранением введённого пользователем val; "
        "добавить отсутствующие. Защищённые имена (например, тип соединения и схема) не удаляются этим правилом.",
    )

    _heading(doc, "6. Связность данных и типичные сценарии пересчёта", 1)
    _p(
        doc,
        "Пересчёт носит итеративный характер с точки зрения пользователя: каждое изменение карты "
        "снова прогоняется через весь конвейер. Поэтому важен порядок: сначала справочники и ключевые поля "
        "(тип шва, схема, материал), затем величины, от которых зависят последующие шаги (чувствительность, "
        "аппарат, напряжение), затем зависимые экраны и подсказки расстояний.",
    )
    _p(
        doc,
        "Пример сценария 1: пользователь выбирает уровень качества B и задаёт радиационную толщину 18 мм — "
        "ControlSensitivityChanger выставляет K из таблицы; далее схема панорамного контроля может отфильтровать "
        "аппараты по Φ; подсказка f будет согласована с K и геометрией.",
    )
    _p(
        doc,
        "Пример сценария 2: смена материала и толщины меняет U_max в voltage_tube — сужается список ИИИ, "
        "обновляется строка напряжения, пересчитываются рекомендации по экранам.",
    )
    _p(
        doc,
        "Пример сценария 3: смена типа сварного соединения — ParamsByWeldedJointFromDb перестраивает набор полей "
        "в «Объект контроля», после чего CircButtWeldBeadFromThicknessTable может заполнить величины валика, "
        "если тип и толщина попадают в таблицу.",
    )

    _heading(doc, "Приложение А. Поля начального шаблона карты (crateTemplateTechCars)", 1)
    _p(
        doc,
        "Ниже перечислены параметры, которые гарантированно присутствуют в шаблоне до запуска "
        "пайплайна. После process часть полей может быть переименована, дополнена options или "
        "значениями из базы — это отражено в соответствующих changers.",
    )
    _heading(doc, "А.1. Блок «ОПЕРАЦИОННАЯ ТЕХНОЛОГИЧЕСКАЯ КАРТА…»", 2)
    for line in [
        "Шифр.",
        "Уровень качества (варианты A, B, C).",
        "НАИМЕНОВАНИЕ ОРГАНИЗАЦИИ.",
        "НОМЕР ЧЕРТЕЖА (ЭСКИЗА).",
        "НАИМЕНОВАНИЕ ОБЪЕКТА.",
        "МЕТОДИКА КОНТРОЛЯ.",
        "НОРМАТИВНЫЕ ДОКУМЕНТЫ.",
    ]:
        _p(doc, line, bullet=True)

    _heading(doc, "А.2. Блок «Объект контроля»", 2)
    for line in [
        "Тип сварного соединения — ключевой параметр; от него зависят схема, набор геометрических полей "
        "и строки справочников.",
        "Далее в процессе работы пайплайна добавляются параметры из БД (диаметр, толщина, размеры валика и др.) "
        "согласно типу шва.",
    ]:
        _p(doc, line, bullet=True)

    _heading(doc, "А.3. Блок «ИСХОДНЫЕ ДАННЫЕ»", 2)
    for line in [
        "схема просвечивания — определяет, какие расчётные changers активируются.",
        "Радиационная толщина, мм — используется в чувствительности, прямолинейной схеме ЦР, косвенно влияет на выбор эталона.",
        "Чувствительность контроля, мм — рассчитывается по таблице или задаётся пользователем; вход для геометрии панорамы и схем 7.2/7.3.",
        "Тип и номер эталона чувствительности — вывод SensitivityEtalonByMaterial.",
        "Расстояние от ИИИ до поверхности контролируемого сварного соединения, мм — в карте хранится подсказка/placeholder.",
        "ИИИ Рентгеновский аппарат — в процессе может быть переименован в «ИИИ»; options фильтруются по напряжению и схеме.",
        "Размер фокусного пятна ИИИ, мм — подставляется из выбранного аппарата.",
        "Напряжение на рентгеновской трубке, не более, кВ — предел из voltage_tube, не копируется из аппарата.",
        "Тип радиографической пленки D-7 (AGFA) — из БД.",
        "Тип и толщина усиливающего экрана, мм — пороговая логика по кВ.",
        "Толщина защитного экрана, мм — пороговая логика по кВ.",
        "Количество участков, шт.",
        "Размеры оценочного участка (ширина × длина), мм.",
        "Размеры радиографической пленки (ширина × длина), мм.",
    ]:
        _p(doc, line, bullet=True)

    _heading(doc, "А.4. Блок «ПЕРЕЧЕНЬ ОПЕРАЦИЙ РК»", 2)
    _p(
        doc,
        "Изначально пустой; если в базе есть операции для выбранного list_id, блок полностью "
        "пересобирается: каждая операция — отдельный параметр с val/val2.",
    )

    _heading(doc, "Приложение Б. Словарь обозначений в формулах расстояний", 1)
    for line in [
        "D — наружный диаметр трубы, мм.",
        "S_st или «толщина стенки» — номинальная толщина стенки в сечении кольца, мм.",
        "d — внутренний диаметр: d = D − 2·S_st.",
        "K — чувствительность контроля (из поля карты), мм.",
        "Φ — эквивалентный максимум фокусного пятна, извлекаемый из данных аппарата/поля карты.",
        "Q — уровень качества изображения в терминах A/B/C; в прямолинейной ЦР соответствует SA-R/SB-R/SC-R.",
        "S или S_image — множитель класса изображения в формулах (1.2 / 1.1 / 1.5 в зависимости от сценария).",
        "t — радиационная толщина, мм.",
        "b — расстояние от плоскости приёмника до поверхности металла (если задано отдельным полем), мм.",
        "f — расстояние от источника (ИИИ) до поверхности контролируемого соединения; в интерфейсе задаётся подсказкой.",
    ]:
        _p(doc, line, bullet=True)

    _heading(doc, "7. Ограничения и допущения, зафиксированные в коде", 1)
    for line in [
        "Часть нормативных коэффициентов и таблиц перенесена в код буквально; при обновлении НД необходима "
        "ревизия соответствующих модулей.",
        "При отсутствии данных (None, неразборчивый ввод) многие changers намеренно «молчат» — не пишут число, "
        "чтобы не вводить пользователя в заблуждение.",
        "Парсинг чисел допускает запятую как десятичный разделитель и извлечение первого числа из строки.",
        "Метрики качества архитектуры (Мартин) к предметной логике не относятся; данный документ описывает "
        "именно содержательную часть слоя services.",
    ]:
        _p(doc, line, bullet=True)

    _heading(doc, "8. Заключение", 1)
    _p(
        doc,
        "Слой бизнес-логики проекта представляет собой совокупность правил преобразования "
        "операционной технологической карты радиографического контроля. Его содержание определяется "
        "набором модулей в services/Changers и orchestration в TechCardService/PipeLine. "
        "Документ может служить приложением к пояснительной записке: он фиксирует входы, выходы, "
        "табличные зависимости и расчётные соотношения в терминах предметной области.",
    )

    return doc


def main() -> None:
    doc = build_document()
    OUTPUT.parent.mkdir(parents=True, exist_ok=True)
    doc.save(OUTPUT)
    print(str(OUTPUT))


if __name__ == "__main__":
    main()
